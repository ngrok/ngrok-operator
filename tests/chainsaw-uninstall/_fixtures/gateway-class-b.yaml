# GatewayClass for operator B in multi-operator tests
# Note: GatewayClass is cluster-scoped (no namespace)
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: ngrok-b
spec:
  controllerName: ngrok.com/gateway-controller-b
