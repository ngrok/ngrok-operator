# Values overlay to enable bindings feature
# Use with: --values ../_fixtures/values-bindings-enabled.yaml

agent: {}
apiManager: {}
features:
  bindings:
    enabled: true
    endpointSelectors:
    - "true" # Accept all kubernetes-bound endpoints
