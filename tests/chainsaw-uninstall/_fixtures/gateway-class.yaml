# GatewayClass for ngrok Gateway API support
# Note: GatewayClass is cluster-scoped (no namespace)
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: ngrok
spec:
  controllerName: ngrok.com/gateway-controller
