# Scenario: multi-ingressclass-delete-policy - Operator A
# Watches all namespaces, scoped by ingress class

drainPolicy: Delete
installCRDs: false
# No watchNamespace - watches all namespaces

ingress:
  controllerName: k8s.ngrok.com/ingress-controller-a
  ingressClass:
    name: ngrok-a
