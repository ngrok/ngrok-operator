# Gateway managed by the third-party controller.
# HTTPRoutes that target this Gateway must be ignored by the ngrok operator.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: other-gateway
spec:
  gatewayClassName: other-controller
  listeners:
    - name: http
      port: 80
      protocol: HTTP
