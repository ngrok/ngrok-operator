suite: test bindings configuration
templates:
- bindings/configuration.yaml
tests:
- it: Should match snapshot
  set:
    bindings:
      enabled: true
      name: "test-1"
  asserts:
  - matchSnapshot: {}
