suite: test bindings forwarder deployment
templates:
- bindings-forwarder/deployment.yaml
- bindings-forwarder/role.yaml
- configmap.yaml
set:
  features:
    bindings:
      enabled: true
tests:
- it: Should match snapshot
  template: bindings-forwarder/deployment.yaml
  asserts:
  - matchSnapshot: {}
- it: Should set default bindingsForwarder.updateStrategy.type to RollingUpdate in values.yaml
  set:
    bindingsForwarder:
      updateStrategy:
        type: RollingUpdate
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.strategy.type
      value: RollingUpdate
- it: should create a deployment if features.bindings.enabled is true
  template: bindings-forwarder/deployment.yaml
  asserts:
  - isKind:
      of: Deployment
  - isAPIVersion:
      of: apps/v1
  - equal:
      path: metadata.name
      value: RELEASE-NAME-ngrok-operator-bindings-forwarder
- it: Defaults to having "soft" pod anti-affinity
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.template.spec.affinity.podAntiAffinity
      value:
        preferredDuringSchedulingIgnoredDuringExecution:
        - podAffinityTerm:
            labelSelector:
              matchLabels:
                app.kubernetes.io/component: bindings-forwarder
                app.kubernetes.io/instance: RELEASE-NAME
                app.kubernetes.io/name: ngrok-operator
            topologyKey: kubernetes.io/hostname
          weight: 1
- it: Adds .Values.defaults.podLabels to the forwarder deployment podspec
  set:
    defaults:
      podLabels:
        labelKey1: labelValue1
        labelKey2: labelValue2
  template: bindings-forwarder/deployment.yaml
  asserts:
  - isSubset:
      path: spec.template.metadata.labels
      content:
        labelKey1: labelValue1
        labelKey2: labelValue2
- it: Does not add .Values.defaults.podLabels to the forwarder deployment's selector
  set:
    defaults:
      podLabels:
        labelKey1: labelValue1
        labelKey2: labelValue2
  template: bindings-forwarder/deployment.yaml
  asserts:
  - isNotSubset:
      path: spec.selector.matchLabels
      content:
        labelKey1: labelValue1
        labelKey2: labelValue2
- it: Supports selecting which nodes the bindings forwarders should run on with node selectors
  set:
    bindingsForwarder:
      nodeSelector: &nodeSelector
        disktype: ssd
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.template.spec.nodeSelector
      value: *nodeSelector
- it: Supports setting tolerations on the bindings forwarder pods
  set:
    bindingsForwarder:
      tolerations:
      - &toleration
        key: "key1"
        operator: "Equal"
        value: "value1"
        effect: "NoSchedule"
  template: bindings-forwarder/deployment.yaml
  asserts:
  - contains:
      path: spec.template.spec.tolerations
      content: *toleration
- it: Supports setting topologySpreadConstraints on the bindings forwarder pods
  set:
    bindingsForwarder:
      topologySpreadConstraints:
      - &tsc
        maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: foo
        matchLabelKeys:
          - pod-template-hash
  template: bindings-forwarder/deployment.yaml
  asserts:
  - contains:
      path: spec.template.spec.topologySpreadConstraints
      content: *tsc
- it: Supports setting resources on the bindings forwarder pods
  set:
    bindingsForwarder:
      resources: &resources
        requests:
          memory: "64Mi"
          cpu: "250m"
        limits:
          memory: "128Mi"
          cpu: "500m"
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.template.spec.containers[0].resources
      value: *resources
- it: Sets terminationGracePeriodSeconds by default
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.template.spec.terminationGracePeriodSeconds
      value: 30
- it: Allows overriding terminationGracePeriodSeconds for the bindings forwarder
  set:
    bindingsForwarder:
      terminationGracePeriodSeconds: 75
  template: bindings-forwarder/deployment.yaml
  asserts:
  - equal:
      path: spec.template.spec.terminationGracePeriodSeconds
      value: 75
- it: passes only identity args and the config file
  template: bindings-forwarder/deployment.yaml
  asserts:
  - contains:
      path: spec.template.spec.containers[0].args
      content: --config=/etc/ngrok-operator/bindingsForwarder.yaml
  - notMatchRegex:
      path: spec.template.spec.containers[0].args[*]
      pattern: '^--region='
- it: mounts the operator config
  template: bindings-forwarder/deployment.yaml
  asserts:
  - contains:
      path: spec.template.spec.volumes
      content:
        name: operator-config
        configMap:
          name: RELEASE-NAME-ngrok-operator-config
- it: deep-merges defaults into the component, with the component winning
  template: bindings-forwarder/deployment.yaml
  set:
    defaults:
      podAnnotations:
        shared: "from-defaults"
        overridden: "from-defaults"
      tolerations:
        - key: shared-toleration
    bindingsForwarder:
      podAnnotations:
        overridden: "from-forwarder"
        forwarderOnly: "from-forwarder"
      tolerations:
        - key: forwarder-toleration
  asserts:
    # map keys merge, component wins on collision
    - equal:
        path: spec.template.metadata.annotations.shared
        value: from-defaults
    - equal:
        path: spec.template.metadata.annotations.overridden
        value: from-forwarder
    - equal:
        path: spec.template.metadata.annotations.forwarderOnly
        value: from-forwarder
    # arrays replace wholesale rather than appending
    - equal:
        path: spec.template.spec.tolerations
        value:
          - key: forwarder-toleration
- it: rolls only this component when its own config changes
  template: bindings-forwarder/deployment.yaml
  set:
    bindingsForwarder:
      log:
        level: debug
  asserts:
    - exists:
        path: spec.template.metadata.annotations["checksum/config"]
- it: does not render in one-click demo mode, which has no authtoken for the forwarder
  template: bindings-forwarder/deployment.yaml
  set:
    apiManager:
      oneClickDemoMode: true
  asserts:
    - hasDocuments:
        count: 0
